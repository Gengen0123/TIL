# macOSでHomebrew経由のPHPをターミナルから実行できるようにした

## ゴール
- `php -v` が通る
- `php -S localhost:8080` でローカルサーバーを起動できる

## 症状
- `php` が見つからない / 想定と違うPHPが起動する（例: XAMPPや古いPHP）

## 確認したこと
### どのphpを参照しているか
```sh
which php
php -v
